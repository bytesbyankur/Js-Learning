//Q. Write a program to find the largest number in an array.

let arr = [1,3,5,2,34,6,7,4,8,91,10]

let max=0;
for(let i in arr){
    if(max<arr[i])[
        max=arr[i]
    ]
}

console.log(`Largest number is ${max}`)